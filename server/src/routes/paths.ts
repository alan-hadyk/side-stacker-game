export enum Path {
  Games = "/games",
  Game = "/games/:game_id",
  Moves = "/moves",
  Players = "/players",
  Player = "/players/:player_id",
  CurrentPlayer = "/players/current",
  SignIn = "/sign-in",
  SignOut = "/sign-out",
  Root = "/",
}
